
    effect(() => {
      const rings = this.rangeRingsSignal();
      if (rings && rings.center) {
        this.engine.updateRangeRings(rings.center, rings.intervals);
      } else {
        this.engine.updateRangeRings([0, 0], []);
      }
    });

